.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

# scratch: cached streamline endpoints for scripts/measure_tract_uncertainty.py
.cache/

# corpus payloads: hundreds of GB, tracked by card + origin URL, not by git
data/sources/*/raw/
data/derived/

# training checkpoints: hundreds of MB each, published to HuggingFace
# (brandonin/ibm-1) with a sidecar. git is the wrong store for weights.
ckpt/
# regenerable decoder/tractography cache
.cache/
# rendered clips and prediction dumps
out/*.pt
out/*.gif
out/*.npy
