# where the bytes for `voxceleb` actually live.  never the bytes themselves.
source_url: "https://www.robots.ox.ac.uk/~vgg/data/voxceleb/"
local_root: null   # set on acquisition
acquired_at: null
bytes: null
