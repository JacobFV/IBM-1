# where the bytes for `libribrain` actually live.  never the bytes themselves.
local_root: /home/brandonin/Documents/win-data/libribrain
acquired_at: '2026-09-04'
files: 163
bytes: 48943755496
gib: 45.58
checksums: deferred -- see checksums.txt
note: bytes live outside the repository and are never committed; this file records
  where they are.
source_url: https://huggingface.co/datasets/pnpl/LibriBrain
