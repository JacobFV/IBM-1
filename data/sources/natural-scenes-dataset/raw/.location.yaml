# where the bytes for `natural-scenes-dataset` actually live.  never the bytes themselves.
source_url: "https://naturalscenesdataset.org/"
local_root: null   # set on acquisition
acquired_at: null
bytes: null
