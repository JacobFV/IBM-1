# where the bytes for `h01` actually live.  never the bytes themselves.
source_url: https://storage.googleapis.com/h01-release/data/20210601/
local_root: /home/brandonin/Documents/win-data/h01
acquired_at: '2026-09-05'
files: 104
bytes: 19807850289
gib: 18.447
checksums: deferred -- fetched over https from a public bucket, byte counts recorded here
note: 'H01 release 20210601: the c3 soma table, the c3 neuroglancer segment-property summary (per-segment
  excitatory and inhibitory input counts over 46,637 segments), the proofread_104 synapse locations, and
  a prefix of the exported synapse table.  the exported table is 166 avro shards totalling 33 GB and each
  shard is a uniform sample over the whole z range rather than a slab, so a prefix of them is a uniform
  sample of synapses; the sampling fraction is MEASURED against the released whole-volume per-segment
  counts rather than assumed to be n/166.'
