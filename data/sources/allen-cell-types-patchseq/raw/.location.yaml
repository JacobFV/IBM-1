# where the bytes for `allen-cell-types-patchseq` actually live.  never the bytes themselves.
source_url: http://api.brain-map.org/api/v2/data/query.json?criteria=model::ApiCellTypesSpecimenDetail
local_root: /home/brandonin/Documents/win-data/allen-cell-types-patchseq
acquired_at: '2026-09-05'
files: 1
bytes: 3975943
gib: 0.004
checksums: deferred -- fetched over https from a public bucket, byte counts recorded here
note: 'the Allen Cell Types Database specimen-detail table, fetched anonymously from the RMA endpoint:
  2,333 patch-clamp specimens, 1,920 mouse and 413 human, with membrane time constant, resting potential,
  input resistance, adaptation index, f-I slope, cortical layer and Cre line per cell.  allensdk was not
  installed: the REST endpoint returns the same table in one request and adds no dependency.'
