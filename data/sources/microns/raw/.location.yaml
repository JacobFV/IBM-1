# where the bytes for `microns` actually live.  never the bytes themselves.
source_url: https://storage.googleapis.com/allen-minnie-phase3/public_data/
local_root: /home/brandonin/Documents/win-data/microns
acquired_at: '2026-09-05'
files: 5
bytes: 342301890
gib: 0.319
checksums: deferred -- fetched over https from a public bucket, byte counts recorded here
note: 'MICrONS minnie65 public release, materialization 1078: the derived connectivity product only.  the
  EM volume itself is petavoxel-scale and was deliberately not fetched -- what is here is the proofread-axon
  table, the automatic and manual cell type tables with soma positions, and every output and input synapse
  of the proofread axons.  CAVEclient could not be used: the CAVE info service and the minnie65_public
  materialization both redirect to a Google OAuth wall, so the public GCS bucket was read directly and
  anonymously over https.'
