# ibm-1 source card. schema: data/schema/source.schema.json
id: cinesync
title: CineSync -- the modelling counterpart to CineBrain
provider: unresolved -- named in PROMPT.md §53.1
citation: 'CineBrain / CineSync, arXiv:2503.06940 (Gao et al.). Identified 2026-08-17. NOTE THAT ONE PAPER
  COVERS BOTH NAMES: CineSync appears only inside the CineBrain paper rather than as its own reference,
  which is why these are two cards over one artifact rather than two artifacts. See notes/literature/cinebrain-cinesync.md,
  which is deliberately one note for the pair.'
urls:
- https://arxiv.org/abs/2503.06940
access: watch
licence:
  name: unresolved
  redistribution: unresolved
  commercial: unresolved
  notes: Model weights, code and training data commonly carry THREE different licences, and a permissive
    code licence says nothing about the weights. Unresolved.
binding: unbound
population:
  n: null
frames:
- unresolved
streams:
- name: model_dataset_pairing
  kind: measured
  via: null
  systematic_bias: '§48 and registry/README: TEACHER IS NOT OBSERVATION. This model''s representations
    are a discrepancy-aware forcing CANDIDATE, never automatically a subject likelihood. §29: influence
    starts low and is earned through held-out prediction. A model released alongside its own dataset is
    a specific hazard: the evaluation and the training data share provenance, so a reported result is
    not an independent test. §46''s requirement for a held-out arm that could have failed needs checking
    here specifically.'
  band_hz:
  - null
  - null
  temporal_support: unresolved
  spatial_support: unresolved -- paired with the cinebrain dataset card, which is itself unverified
absent:
- everything_unverified
use:
- distil
forbidden:
- cinesync/* -> observation-role likelihoods
- cinesync/* -> B-WIN's state ontology or support definitions
split:
  group_by:
  - participant
  - stimulus
provenance:
  imported_from: win-registry
  card: cinesync
  imported_at: '2026-09-04'
  index: data/index/win/cards.jsonl
distillation:
  reported_accuracy:
    metric: unresolved
    value: null
    on_streams:
    - model_dataset_pairing
    source: not yet extracted from the paper
    verified: false
  precision_model:
    form: unresolved
    ood_inflation_prior: unset -- must be a prior, not a constant
    error_rank: unresolved -- a teacher's residuals covary across everything it writes
    notes: distilled values enter as evidence and must carry calibrated precision; see ARCHITECTURE.md,
      distillation precision
    correlated_fraction: null
    effective_constraints: null
